{
  "name": "nextbridge-classroom-kit",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test",
    "smoke": "node scripts/smoke.mjs",
    "validate": "node scripts/validate.mjs",
    "check": "npm run validate && npm test && npm run smoke"
  }
}
